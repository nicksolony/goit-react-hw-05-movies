// import { lazy } from "react";
// import { Route, Routes } from "react-router-dom";

import { AppContainer } from "./App.styled";

export const App = () => {
  return (
    <AppContainer>
      testing setup
    </AppContainer>
  );
};
